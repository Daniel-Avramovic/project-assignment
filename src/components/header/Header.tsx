import React, { FC } from 'react';
import './header.css';

const Header: FC = () =>{
    return(
        <header data-testid="header" >
            Project Assingment
        </header>
    )
};

export default Header;